<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorButtonNormal">@color/colorButtonNormal</item>
    </style>

    <!-- This is the Button Style -->
    <style name="buttonStyle">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- This is the Questions and Text Views' Style -->

    <style name="questions">
        <item name="android:padding">@dimen/paddingUniversal</item>
        <item name="android:textSize">@dimen/textSizeQuestions</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/colorQuestions</item>
    </style>

    <!-- This is the Check Boxes and Radio Buttons' Style -->

    <style name="checkBoxes">
        <item name="android:padding">@dimen/paddingUniversal</item>
        <item name="android:textSize">@dimen/textSizeAnswers</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/colorAnswers</item>
    </style>






</resources>
